---
import Layout from "@layouts/Layout.astro";
import Footer from "@components/Footer.astro";
import Counter from "@components/Counter.astro";
import counter from "@content/counter.json";
import { homepage } from "@content/homepage";
import Features from "@components/Features.astro";
---

<Layout>
  <section class="grid py-10 place-items-center text-center">
    <h1 class="sm:text-4xl text-3xl font-bold mb-6 dark:text-white">Food Web Complexity</h1>
    <h2 class="sm:text-2xl text-xl font-bold mb-6 dark:text-white">beyond species counting</h2>
    <p>A NetSci2023 satellite</p>
  </section>
  <section class="sm:text-xl text-xl mb-6 dark:text-white">
    <ul class="list-disc">
      <li>Where are we at in defining and assessing food web complexity?</li>
      <li>what are specific definitions in use in particular aspects of ecological network research? What are their limitations? What their strength?</li>
      <li>What are the relations between novel complexity definitions and the fundamental processes of ecological networks? What advantages do they offer?</li>
      <li>How can practitioners estimate complexity in food webs? What data is necessary? What software is available?</li>
      <li>Why should we care about new definitions? What dothey reveal about ecological networks, and what actions do they suggest?</li>
    </ul>
  </section>
  {homepage.map((homepage) => <Features title={homepage.title} icon={homepage.icon} content={homepage.content} />)}
  <section class="py-20 sm:flex sm:items-center sm:justify-evenly text-center sm:text-left">
    {
      (
        <ul class="grid sm:grid-cols-3 gap-16">
          {counter.map((counter) => (
            <Counter number={counter.number} label={counter.label} />
          ))}
        </ul>
      )
    }
  </section>
  <Footer />
</Layout>
